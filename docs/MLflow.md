
MLflow: to run locally:
1. Install mlflow (`pip install mlflow`).
2. Start the server: `mlflow ui` (default http://127.0.0.1:5000)
3. Run `python scripts/train_mlflow_example.py` to create runs and log model.
